（好像修好了） 多显示屏时，大小和位置识别不正确，目前观测到我开双屏时，主显示器的边缘提示只有上和左侧有，通过观察顶部进度条可以发现，整体右偏移了（进度条在顶部偏右的位置）

---

1. 需要给应用程序、任务栏和标题栏都加上图标，文件在【F:\codex\ElasticBreath\ElasticBreath.App\Resource\icon.ico】
2. 允许最小化，不要每次最小化就自动关掉窗口
3. 设置里面允许设置【点击关闭主界面后：·结束运行 | ·收起到任务栏】
4. 左键单击任务栏要直接唤起主界面，不要双击才能打开
5. 自动转换模式有几个细节问题：
    - 智能检测的阈值需要允许在设置中调整，以秒为单位
    - 智能检测时间也要算进步骤执行时间，比如无操作30秒后就算进入休息，那么，起始计时就是从30秒开始
    - 智能由idle状态进入工作状态似乎有问题，应该和从【休息】状态进入【工作】状态类似，有一个可以配置的阈值，并在期间如果没有操作就归零。当前只要进入idle状态马上就会开始5秒的计时，完全无法终止。
    - 目前状态机的逻辑还有些不清晰，请你写一份状态机文档，详细说明所有状态的转换规则。
    - 锁屏应当认为是进入休息而不是暂停工作
6. 将所有时间相关设置项改为以秒为单位（文本提示也要改），且允许填写算术式（包含+-*/()0123456789的字符串）并保证计算的鲁棒性

---

1. 设置虽然支持了算术，但是需要保存时也按用户写的算术式保存，方便阅读
2. 所有待处理切换都要显示在UI，不只是idle到working
3. hard警示的边缘渗透宽度也应该逐步增长到配置上限，就像Overtime区间一样
4. 工作状态时，【开始工作】按钮应该显示【暂停】按钮，按下后改为【继续】按钮。【开始休息】按钮同理
5. 休息时长短于【最短休息时长】时，认为此次休息无效，如果再切换为工作状态就继续上一次工作状态的计时加上此次不满足要求的休息时长（认为一直在工作）。若此次休息切换到非工作状态（如暂停或者idle）则保持当前行为，认为正常休息了。
6. 锁屏再回来似乎没有触发正确的检测。收到锁屏信号就直接切换到idle好了。
7. 写的时候加上合适的注释，目前代码缺乏注释。
8. 所有智能转换有阈值的，只要中途发生不满足条件（如工作到一半歇了20秒没动，然后又动起来。或者是休息时动了两下鼠标）的就计时清零，并中途计时到多少了记得显示在UI上。这一条要求是有可能已经完成了的，需要你检查是否实现。
9. 【离开判定】必须比【休息超时判定阈值】要长，并只在休息时进行判定
10. 请你充分阅读代码后再修改