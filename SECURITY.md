# 安全策略

## 安全策略概述

弹性呼吸（ElasticBreath）是一款本地运行的休息引导工具，高度重视用户隐私与系统安全。本文件说明项目的安全策略、漏洞报告流程，以及与安全相关的设计说明，帮助用户和贡献者了解应用在安全方面的边界与承诺。

## 报告漏洞

请按问题类型选择合适的渠道：

- **安全漏洞**：请**不要**通过公开 Issue 报告，以免漏洞在修复前被公开利用。请使用 GitHub 仓库的 "Report a vulnerability"（私密 Security Advisory）私下报告。
- **普通 Bug / 功能建议**：欢迎直接提公开 Issue。

报告时请尽可能包含：受影响版本、复现步骤、潜在影响，以及建议的修复方向（如有）。

**预期响应时间**：

- 收到报告后：3 个工作日内确认收到。
- 初步评估：7 个工作日内反馈评估结果与修复计划。
- 修复发布：视严重程度，通常在确认后 30 天内发布修复版本。

请在漏洞修复并发布新版本之前，不要公开披露漏洞细节。我们会在致谢列表中感谢负责任的报告者（除非您要求匿名）。

## 安全相关设计说明

本应用通过 Win32 P/Invoke 与 Windows 系统交互，相关调用及其用途说明如下：

- **`GetLastInputInfo`**：用于空闲检测，仅读取系统最后一次输入的时间戳，**不记录**任何按键内容或鼠标操作内容。
- **`GetForegroundWindow` / `GetWindowThreadProcessId`**：用于检测当前前台窗口所属进程，以便在远程控制工具（如向日葵、ToDesk、TeamViewer 等）处于活动状态时过滤其输入，避免误判远程会话为本地活动。仅读取进程标识，**不读取**窗口内容或进程内存。
- **`MonitorFromPoint` / `GetDpiForMonitor`**：用于查询显示器 DPI 缩放系数，将屏幕物理像素坐标换算为 WPF 设备无关单位，保证多屏异 DPI 下副屏呼吸边框与通知窗口精确定位。仅读取显示器 DPI 数值，**不读取**任何用户数据。

应用**不会**进行以下操作：

- 记录键盘按键或鼠标点击的内容；
- 截取屏幕内容并外发；
- 注入输入或修改其他进程的行为；
- 访问网络。

> 关于“访问网络”的边界说明：关于窗口中的仓库链接以 WPF `Hyperlink` 形式呈现，点击后通过 `Process.Start` 以 `UseShellExecute = true` 委托给系统默认浏览器打开，由浏览器自身完成网络访问，**程序进程不发起任何 HTTP 请求**，也不引入 `HttpClient` / WebSocket 等网络客户端。这与“不引入任何网络请求代码”的约束一致。

设置与状态数据均存储在本地 `config/settings.json`，文件权限由用户账户控制。

## 数据收集声明

弹性呼吸**不收集任何用户数据**：

- 无遥测（Telemetry）；
- 无崩溃数据自动上报（崩溃日志仅写入本地，由用户自行决定是否提交）；
- 无网络请求；
- 无第三方分析或广告 SDK。

所有配置与运行数据均保存在用户本地文件系统中。

## 支持版本

安全修复仅针对**最新版本**（当前为 0.9.x）提供。旧版本不再接收安全更新，请始终使用最新发布版本。

如需查看当前版本，请参阅应用内"关于"信息或 Releases 页面。
