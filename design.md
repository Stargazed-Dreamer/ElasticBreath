
# 软件设计书：弹性呼吸（ElasticBreath）
## 1. 产品概述
弹性呼吸 是一款面向长时间电脑使用者的 Windows 效率与健康工具。它通过智能检测用户活动状态，自动管理“工作－休息”周期。软件摒弃“倒计时炸弹”模型，采用“水位预警模型”，在不强制打断关键操作的前提下，利用边缘视觉与弹性时间窗口，提供不可习惯化的休息引导。
## 2. 核心设计原则
1. **永不阻断中心视野**：绝对禁止模态弹窗、禁止抢占焦点、禁止黑屏遮挡。
2. **无“关闭”出口（针对悬浮提示）**：边缘光晕、角落触发圆等休息提示元素不设关闭按钮，用户只能“完成”它，无法随手打消。
3. **弹性窗口而非硬性闹钟**：在预设的弹性区间内提供休息入口，由用户决定精确的切入和切出时刻。
4. **双向智能感知**：自动识别离开与归来，以轻量确认代替手动启停。
5. **渐进压迫**：视觉反馈随时间逐步增强，将“该休息了”植入潜意识。
6. **环境与状态适配**：单屏极简，多屏增强，全屏应用下隐匿，不干扰游戏。
## 3. 状态机与核心逻辑
开发需严格按照以下状态机编写，所有视觉表现均已明确绑定：
```text
[空闲状态 Idle]
   │
   ├─(用户手动点击主窗口“开始工作”)─→ [专注计时 Working]
   │
   └─(检测到输入且5秒内未点击取消通知)─→ [专注计时 Working]
[专注计时 Working]
   │
   ├─(时间 < 最短工作阈值) ─→ 表现：无UI（或仅显示已开启的顶部进度条）
   │
   ├─(最短工作阈值 ≤ 时间 < 最长工作阈值) ─→ 表现：预警区，边缘橙色光晕（若开启）
   │   └─(用户触发角落停留 / 点击主窗口按钮) ─→ [恢复计时 Resting]
   │
   ├─(时间 ≥ 最长工作阈值) ─→ 表现：硬性区，边缘红色光晕（若开启）
   │   └─(用户触发角落停留 / 点击主窗口按钮) ─→ [恢复计时 Resting]
   │
   └─(无操作空闲 ≥ 离开判定阈值) ─→ [暂停 Paused]
[暂停 Paused]
   │
   ├─(视觉表现) ─→ 无边缘光晕（与隐藏态一致）。
   │
   ├─(检测到输入) ─→ 弹出恢复提示通知（5秒倒计时）
   │   ├─(5秒内无处理) ─→ [专注计时 Working] (接续之前的时间)
   │   └─(5秒内点击通知任意处) ─→ 保持 [暂停 Paused]
   │
   └─(用户手动点击主窗口“停止”) ─→ [空闲状态 Idle]
[恢复计时 Resting]
   │
   ├─(时间 < 默认休息时长) ─→ 表现：基础区，边缘绿色光晕
   │
   ├─(默认休息时长 ≤ 时间 < 弹性超时) ─→ 表现：弹性区，绿色光晕变亮
   │
   └─(时间 ≥ 弹性超时) ─→ 表现：超时区，绿色光晕开始闪烁
       │
       ├─(用户触发角落停留 / 点击主窗口按钮) ─→ [专注计时 Working] (清零重新计时)
       │
       └─(检测到持续超过30秒的密集键盘/鼠标输入) ─→ [专注计时 Working] (智能判定提前结束休息)
```
*注：一次“完整休息”的定义为：休息时长 ≥ 配置中的“最短有效休息时长”。仅完整休息可重置“每日推迟上限”。*
## 4. 核心机制：弹性时间窗口
废除固定时间点，引入窗口期概念（以默认值为例）：
* **专注期**：安全区（0~35分，无干预） → 预警区（36~45分，弹性提供入口） → 硬性区（46分+，高压视觉但不阻断）。
* **恢复期**：基础区（0~5分，引导休息） → 弹性区（5~8分，宽容时段） → 超时区（8分+，提示回归）。
## 5. UI/UX 设计规格
### 5.1 主窗口（标准系统窗口）
* **定位**：等同于普通软件的主界面，可最小化、可关闭（关闭后缩至托盘），**非常显控制塔**。用户可自由拖动至任意屏幕。
* **工作态UI**：淡绿色背景；核心为巨大圆环进度条（占窗口短边40%，线宽4px，颜色随时间绿→黄→红渐变）；圆环内显示已工作时长（如 `45:20`，Segoe UI Light字体）；底部状态栏左下显示今日工作/休息累计，右下显示智能检测状态（`● 监测中`）。
* **干预态UI**：圆环停止转动，变红色脉冲，中心文字变为 `STOP & REST`。
* **核心按钮**：开始工作、开始休息、暂停提醒（需二次确认，确认后关闭所有计时和提醒，相当于软关闭软件，可再次点击开启）。
* **附加功能**：包含休息期间的具体微动作文案指引（如“双手十指交叉，向外翻转推伸...”），以及设置页面的入口。
### 5.2 悬浮视觉反馈（用户可独立开关，均不影响下方鼠标/键盘操作）
若用户全部关闭，则仅依赖主窗口进行状态感知。
* **选项A：顶部细长进度条**
  * 屏幕顶部中央，10px高。颜色随工作剩余时间从绿渐变红。鼠标悬停显示具体时间。
* **选项B：边缘呼吸光晕**
  * 屏幕四周边缘向内渐变的光晕。最大渗透宽度不超过160像素。
  * **预警态**：暗橙缓慢过渡至亮橙，3秒一次呼吸明暗变化。
  * **硬性态**：警示红，1秒一次呼吸（防癫痫，不伴随刺耳声音）。
  * **休息态**：清新绿色。
  * **暂停态**：无视觉表现。
### 5.3 交互方式（如何触发状态切换）
* **方式一：主窗口按钮**（适用于多屏模式或将主窗口置于一侧时）。
* **方式二：左上角悬停触发**（适用于单屏极简模式）。在存在光晕或进度条期间，鼠标移至屏幕左上角停留1.5秒，浮现半透明实心圆（圆心在左上角、用户可见右下角四分之一，半径最大20px，出现/收回带弹性），悬停期间由灰渐变为主题绿，变绿瞬间静默切入下一状态，无需点击；切换后圆保持绿色，移开鼠标后收回。仅左上角参与触发——右上角邻近关闭按钮、右下角为"显示桌面"热区、左下角可能是开始菜单。
### 5.4 智能状态切换通知（非模态，可与光晕并存）
* **空闲→专注**：屏幕右上角细长通知：“欢迎回来，要开始工作吗？5秒后开始工作计时 [任意点击本通知取消]”。
* **专注→空闲**：屏幕右上角非模态气泡：“检测到你已离开，5秒后工作计时暂停 [任意点击本通知取消]”，5秒后自动消失。
### 5.5 多显示屏自适应策略
* **动态判定**：启动时及运行中实时检测显示器拓扑。
  * **优先级1**：用户在设置中手动指定的显示器（一旦指定，永久覆盖）。
  * **优先级2**：Windows系统设置中的“主显示器”。
  * **优先级3**：当前鼠标所在的屏幕。
* **多屏增强**：主屏浮层显示在主控屏上；当鼠标移到副屏工作时，鼠标所在副屏与主控屏同时显示呼吸边框，两块屏幕都能感知状态（可开关）。
* **插拔适配**：多变单时自动切回单屏UI逻辑，无闪烁。
## 6. 智能检测模块
* **底层调用**：优先使用 `GetLastInputInfo` API，采样频率每2秒1次。
* **判定标准**：键盘按键、鼠标移动 ≥ 5像素、鼠标点击。
* **兼容性**：
  * 虚拟机/远程桌面：检测输入事件来源，避免宿主空闲误判。
  * 快速锁屏/睡眠：监听 `SessionSwitch` 事件，立即视为离开并保存状态，唤醒后按离开/归来逻辑处理。
  * 双用户切换：每个Windows用户会话独立存储计时状态。
## 7. 异常处理与技术边界
* **全屏游戏兼容（主推方案）**：优先使用 Game Bar 接口、NVIDIA Freestyle 或 AMD Overlay 等官方安全接口绘制光条，绝不使用任何可能触发反作弊的注入操作。
* **全屏游戏兼容（拓展备选方案）**：若官方接口均失效，尝试使用安全的窗口置顶属性（`WS_EX_TRANSPARENT | WS_EX_LAYERED`），但不采用高频（如每秒）强行置顶以防被反作弊拦截。
* **最低回退方案**：全屏独占且无法覆盖时，回退到任务栏图标闪烁 + 可选的每隔5秒短促提示音。
* **性能红线**：光晕渲染帧率严格限制在 ≤ 15 FPS，使用 Direct2D 低功耗层，整体 CPU 占用 < 0.2%。
* **崩溃保护**：自身崩溃时在临时目录写入日志，下次启动提示。
---
## 8. 全局可配置设置项汇总
以下为软件设置页面中需暴露给用户的所有配置项（*斜体为默认值*）：
### 8.1 时间控制参数
| 参数名称 | 默认值 | 说明 |
| :--- | :--- | :--- |
| **最短工作阈值** | *35 分钟* | 专注期安全区结束、预警区开始的节点。 |
| **最长工作阈值** | *45 分钟* | 预警区结束、硬性区开始的节点。 |
| **默认休息时长** | *5 分钟* | 进入休息后建议的基础时长。 |
| **弹性超时时长** | *8 分钟* | 休息超过此时长视为超时，开始闪烁提示回归。 |
| **最短有效休息** | *3 分钟* | 低于此时长的休息不计入“完整休息”（不能重置每日推迟配额）。 |
| **离开判定阈值** | *3 分钟* | 无输入操作达到此时长，自动暂停专注计时。 |
### 8.2 交互与干预参数
| 参数名称 | 默认值 | 说明 |
| :--- | :--- | :--- |
| **推迟冷却时长** | *5 分钟* | 在预警/硬性区选择推迟后，此时间内不再因空闲触发提醒。 |
| **每日推迟上限** | *3 次* | 每日最多允许推迟休息的次数。 |
| **智能恢复倒计时** | *5 秒* | 检测到归来/离开时，右上角通知自动执行状态切换的等待时间。 |
| **角落触发停留时长**| *1.5 秒* | 鼠标停留在屏幕左上角触发状态切换所需的时间。 |
### 8.3 视觉与UI参数
| 参数名称 | 默认值 | 说明 |
| :--- | :--- | :--- |
| **启用顶部进度条** | *关* | 开启后在屏幕顶部显示10px高的状态指示条。 |
| **启用边缘呼吸光晕** | *开* | 开启后在屏幕四周显示渐变光晕。 |
| **启用左上角悬停** | *开* | 开启后鼠标悬停在屏幕左上角可以在工作和休息中切换 |
| **光晕最大渗透宽度**| *80 px* | 光晕向屏幕内延伸的最大距离，防遮挡边缘控件。 |
| **光条/光晕透明度** | *可调* | 适应不同明暗的桌面壁纸。 |
| **全屏隐藏模式** | *关* | 开启后，检测到全屏应用时彻底隐藏所有悬浮UI（含进度条和光晕）。 |
| **副屏呼吸提醒** | *开* | 多屏模式下，鼠标所在副屏与主控屏是否同时呼吸提示。 |
### 8.4 提示音与通知参数
| 参数名称 | 默认值 | 说明 |
| :--- | :--- | :--- |
| **声音提醒** | *关* | 全局声音总开关。 |
| **提醒音量** | *50%* | 独立于Windows系统主音量的音量控制。 |
| **全屏回退提示音** | *关* | 当全屏游戏无法覆盖光晕时，是否启用5秒间隔的短促提示音。 |
### 8.5 多屏与部署参数
| 参数名称 | 默认值 | 说明 |
| :--- | :--- | :--- |
| **主控显示器指定** | *动态判断* | 下拉列表，列出场当前所有显示器。选择“自动”则按 系统主屏→鼠标屏 逻辑动态判断；手动选择某显示器后永久生效，直到用户再次修改。 |
## 9. 交付物与开发规范
* **技术栈**：Windows 10/11 64-bit，推荐 .NET 8 + Win32 API (或 C++ / Rust)。架构需保留对 Linux、macOS 扩展的支持可能性。
* **分发方式**：纯便携版，不写注册表，不强制安装。
* **首次启动向导**：引导设置工作/休息时长，选择悬浮UI组件（进度条/光晕），测试角落触发与光晕外观。
* **用户文档**：需包含行为心理学解释，明确告知用户“为何休息提醒没有关闭按钮，只能通过角落或主窗口完成它”，降低初次使用的认知阻力。
