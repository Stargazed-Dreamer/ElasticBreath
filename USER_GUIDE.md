# ElasticBreath 用户指南

欢迎使用 **弹性呼吸（ElasticBreath）**——一款面向长时间电脑使用者的弹性休息引导工具。本指南面向最终用户，帮助你理解软件的工作方式与各项设置。开发者可参阅 [ARCHITECTURE.md](ARCHITECTURE.md) 与 [STATE_MACHINE.md](STATE_MACHINE.md)。

## 1. 什么是弹性呼吸

弹性呼吸摒弃了传统的“倒计时炸弹”模型，采用**水位预警模型**：它不弹窗、不抢焦点、不黑屏，而是让屏幕边缘随你的工作时长像水位一样缓慢上涨，把“该休息了”这个信号一点点植入潜意识，在你仍然掌控关键操作的窗口期里，提供从容的休息入口。

软件最反直觉的一点是：**所有悬浮提示都没有“关闭”按钮，你只能“完成”它，而不能“打消”它。** 这是刻意为之——

> 心理学上，给一个“关闭”按钮等于递给你一个随手打消提醒的出口，提醒就会被习惯性忽略，最终失效。弹性呼吸只允许你通过两种方式“完成”一次休息引导：在主窗口点击按钮，或把鼠标移到屏幕左上角悬停片刻。完成它，意味着你主动承认并处理了这个信号。这降低了初次使用的认知阻力，却保留了行为矫正的有效性。

我们不强制打断你，但也不让你轻易逃避。这就是“弹性”二字的含义。

## 2. 安装与启动

### 方式一：便携版（推荐）

1. 从 Releases 页面下载便携版 zip。
2. 解压到任意目录（如 `D:\Tools\ElasticBreath`）。
3. 双击 `ElasticBreath.App.exe` 运行。

无需安装、不写注册表、不创建快捷方式。首次运行时会在程序目录下自动创建 `config/settings.json` 存放你的设置。

### 方式二：从源码构建

```bash
git clone <repo-url> ElasticBreath
cd ElasticBreath
dotnet build ElasticBreath.sln
dotnet run --project ElasticBreath.App/ElasticBreath.App.csproj
```

或使用项目内置脚本：

```powershell
./scripts/build-release.ps1
```

> **系统要求**：Windows 10 / 11 64-bit，.NET 8 运行时（使用自包含发布版时无需单独安装）。

## 3. 主窗口详解

主窗口是整个软件的**控制塔**——非常显地告诉你“现在处于什么状态、还能撑多久”。窗口可自由拖动到任意屏幕，可最小化到托盘。

- **圆环进度条**：占据窗口短边约 40%，线宽 4px。颜色随工作时长从**绿色 → 黄色 → 红色**渐变，直观反映压力累积。
- **中心时间显示**：圆环内部以大号字体显示当前周期已用时长（如 `45:20`）。
- **今日累计**：底部状态栏左侧显示今日工作 / 休息累计时长。
- **监测状态**：底部状态栏右侧显示 `● 监测中` 等实时状态。
- **核心按钮**：开始工作 / 开始休息 / 停止 / 暂停提醒 / 设置 / 功能说明 / 关于。
- **关于窗口**：点击“关于”按钮打开。窗口展示当前版本号、作者、仓库链接、许可证摘要与简介，所有信息从程序目录下的 `app.meta.json` 实时读取（便携版改文件即生效，无需重新编译）。仓库链接可点击直接交给系统默认浏览器打开，也可用“复制地址”按钮复制；底部“复制版本信息”一键复制版本/OS/运行时上下文，便于反馈 bug 时附上；“打开崩溃日志文件夹”跳转到 `%TEMP%\ElasticBreath\crash\`（程序未崩溃过时会提示不存在）。
- **干预态**：当进入硬性区时，圆环停止转动并变为红色脉冲，中心文字切换为 `STOP & REST`，视觉上明确提示该休息了。

## 4. 状态说明

弹性呼吸有 4 种主状态：

| 状态 | 含义 | 你会看到什么 |
| :--- | :--- | :--- |
| **空闲 Idle** | 未进入工作周期 | 圆环静止，边缘无光晕；检测到你持续操作后会弹出“即将开始工作”通知 |
| **工作 Working** | 工作计时进行中 | 圆环转动并随时间变色；超过阈值后边缘出现光晕 |
| **休息 Resting** | 休息计时进行中 | 边缘变为清新绿色光晕；超时后绿色变亮并开始闪烁 |
| **暂停 Paused** | 因离开检测中断 | 边缘无光晕（与空闲态一致）；检测到你回来会弹出恢复通知 |

### 压力等级

工作期间按已用时长划分三档（视觉强度递进）：

- **安全 Safe**：未达最短工作阈值——无干预。
- **预警 Warning**：介于最短与最长工作阈值之间——边缘**暗橙色**光晕缓慢呼吸。
- **硬性 Hard**：达到或超过最长工作阈值——边缘**警示红**光晕快速呼吸，圆环进入红色脉冲干预态。

休息期间按已用时长划分三档：

- **基础 Base**：未达默认休息时长——**清新绿色**光晕，引导你安心休息。
- **弹性 Elastic**：介于默认休息与弹性超时之间——绿色光晕变亮，提示可以回归工作。
- **超时 Overtime**：达到弹性超时——绿色光晕开始**闪烁**，且边缘渗透宽度会逐步增长到设置上限，提醒该回来了。

## 5. 如何完成一次休息引导

记住，悬浮提示**没有关闭按钮**。完成它有两种方式：

### 方式一：主窗口按钮

适用于多屏模式，或主窗口被你放在一侧时。点击主窗口的“开始休息”即进入休息；休息好后点击“开始工作”即恢复。

### 方式二：左上角悬停触发

适用于单屏极简模式。当屏幕上出现光晕或顶部进度条期间：

1. 把鼠标移到屏幕**左上角**。
2. **停留约 1.5 秒**（可在设置中调整）。此时角落会浮现一个半透明圆，悬停越久颜色越接近主题绿。
3. 圆完全变绿的瞬间，软件会**静默切换**到下一个状态（工作 ↔ 休息），无需点击。
4. 切换完成后圆保持绿色；把鼠标**移出左上角**后，圆弹性收回。

> **为什么只选左上角？** 右上角容易误触窗口关闭按钮，右下角是"显示桌面"热区，左下角可能是开始菜单，只有左上角最安全。
> 切换后必须先移出左上角，才允许下一次触发——防止手抖连发。

**为什么强制“完成”？** 因为“关闭”会变成习惯性逃避，让提醒失效。两种完成方式都让你**主动承认并处理**了这个信号，这才是有效的行为矫正。

## 6. 智能感知

弹性呼吸具备**双向智能感知**，能自动识别你离开与归来，免去手动启停。

- **自动离开检测**：连续无操作达到“离开判定阈值”（默认 3 分钟），或在工作中无操作达到“无操作自动转休息阈值”（默认 30 秒），软件会暂停工作或自动进入休息。
- **自动归来检测**：检测到你恢复操作后，右上角弹出一张**倒计时通知卡片**（默认 5 秒），提示“5 秒后恢复工作”。**点击卡片任意位置即可取消**本次自动切换。
- **倒计时取消机制**：若在倒计时期间你再次停止操作，本次切换也会自动取消，回到原状态。避免误判。
- **锁屏处理**：监听系统锁屏事件，锁屏时立即进入休息并保存状态；解锁后按活动检测恢复。**锁屏不会丢失今日累计**——今日工作 / 休息时长会一直保留到跨日才重置。
- **远程控制兼容**：当你正在使用 ToDesk / RayLink / SunLogin / TeamViewer / AnyDesk / RustDesk 等远程工具时，软件会识别其前台进程并抑制误判。

## 7. 边缘光晕与顶部进度条

所有悬浮视觉反馈**只作用于边缘**，永不遮挡中心视野，也不影响鼠标 / 键盘操作。颜色含义：

| 颜色 | 出现场景 | 含义 |
| :--- | :--- | :--- |
| **暗橙** | 工作预警区 | 该考虑休息了，弹性窗口已打开 |
| **警示红** | 工作硬性区 | 强烈建议立即休息（仍不强制） |
| **清新绿** | 休息中 | 正在恢复，安心休息 |
| **顶部进度条** | 工作中（可选） | 10px 高细条，颜色随工作剩余时间从绿渐变红 |

- **超时增长**：休息进入超时区后，绿色光晕的边缘渗透宽度会**逐步增长**，直到达到你设置的最大渗透宽度上限（默认 160px）。
- **呼吸节奏**：预警态约 3 秒一次缓慢呼吸；硬性态约 1 秒一次（不伴随刺耳声音，防癫痫）。
- **可独立开关**：顶部进度条、边缘光晕、角落悬停均可单独开关。全部关闭后仅依赖主窗口感知状态。

## 8. 多屏使用

- **主控显示器指定**：在“设置 → 显示”中可选择目标屏幕。选“自动”则按 **首选显示器 → 系统主屏 → 鼠标所在屏** 的顺序动态判定；手动指定某屏幕后永久生效，直到你再次修改。
- **副屏呼吸提醒**：多屏模式下，鼠标移到副屏工作时，副屏与主控屏**同时显示呼吸边框**，两块屏幕都能感知当前状态（可开关，默认开）。
- **热插拔适配**：插拔显示器时软件自动适配。多变单时会自动切回单屏逻辑，无残留闪烁。

## 9. 设置项详解

打开“设置”窗口，配置按以下五组分类，**与 design.md §8 的分类一致**。所有时长字段都支持**算术表达式输入**——例如在“最短工作阈值”里填 `35*60` 而不是 `2100`，配置文件会保留 `35*60` 原文以便日后审阅。

### 9.1 时间参数（秒）

| 设置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 最短工作阈值 | 35\*60 | 工作安全区结束、预警区开始的节点 |
| 最长工作阈值 | 45\*60 | 预警区结束、硬性区开始的节点 |
| 默认休息时长 | 5\*60 | 进入休息后建议的基础时长 |
| 休息超时阈值 | 8\*60 | 超过此时长视为超时，开始闪烁提示回归 |
| 最短有效休息 | 3\*60 | 低于此时长的休息不计为“完整休息”，工作计时不重置 |
| 离开判定阈值 | 3\*60 | 无操作达到此时长判定为离开 |

### 9.2 交互与检测

| 设置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 无操作自动转休息阈值 | 30 秒 | 工作中无操作达到此时长，自动进入休息 |
| 空闲进入工作检测阈值 | 4 秒 | 空闲时连续活动达到此时长，弹出“即将开始工作”倒计时 |
| 休息返回工作检测阈值 | 30 秒 | 休息时持续密集输入达到此时长，自动切回工作 |
| 智能检测采样空隙阈值 | 2 秒 | 判定“是否仍在活动”的采样间隔；超过此空隙视为活动间断 |
| 自动转换倒计时 | 5 秒 | 所有自动切换前右上角通知的等待时间 |
| 左上角停留触发时长 | 1.5 秒 | 鼠标停留在屏幕左上角触发切换所需的时间 |

### 9.3 视觉参数

| 设置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 启用顶部进度条 | 关 | 开启后在屏幕顶部显示 10px 高状态条 |
| 启用边缘呼吸光晕 | 开 | 开启后在屏幕四周显示渐变光晕 |
| 启用左上角悬停触发 | 开 | 开启后鼠标悬停屏幕左上角可在工作 / 休息间切换 |
| 光晕最大渗透宽度 | 80 px | 光晕向屏幕内延伸的最大距离（12~600），防遮挡边缘控件 |
| 光晕透明度 | 0.35 | 适应不同明暗的桌面壁纸（0.1~0.9） |
| 全屏应用时隐藏悬浮层 | 关 | 检测到全屏应用时彻底隐藏所有悬浮 UI |
| 周期性重新置顶 | 关 | 防止 Win+D 等操作把悬浮层推到后面（间隔默认 5 秒） |

### 9.4 显示

| 设置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 主控显示器 | 自动 | 下拉列表选择目标屏幕；选“自动”按系统主屏 → 鼠标屏判定 |
| 副屏呼吸提醒 | 开 | 多屏模式下鼠标所在副屏是否与主屏同时呼吸提示 |
| 点击关闭主界面后 | 收起到任务栏 | 关闭主窗口时的行为：结束运行 / 收起到任务栏 |

### 9.5 声音与通知

| 设置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 启用声音提醒 | 关 | 全局声音总开关 |
| 提醒音量 | 50% | 独立于 Windows 系统主音量（0~100%） |
| 全屏回退提示音 | 关 | 全屏应用无法覆盖光晕时，是否启用 5 秒间隔的短促提示音 |

> **算式输入小贴士**：所有时长类字段都接受形如 `35*60`、`8*60`、`30+30` 的算式，也支持小数与括号（如 `1.5*60`）。设置窗口保存时会自动校验，若算式非法会提示“表达式无效”。

## 10. 常见问题 FAQ

**Q：为什么没有关闭按钮？**
A：这是软件的核心设计。给一个“关闭”等于递给你随手打消提醒的出口，提醒很快会被习惯性忽略而失效。弹性呼吸只允许你通过主窗口按钮或左上角悬停“完成”一次休息引导，让你主动承认并处理这个信号。初次使用会觉得反直觉，但这是行为矫正有效性的关键。

**Q：全屏游戏 / 全屏应用时怎么办？**
A：默认情况下，软件检测到全屏前台应用时会尝试用安全的窗口置顶属性覆盖（绝不使用任何可能触发反作弊的注入）。若仍无法覆盖，会回退到**任务栏图标闪烁** + 可选的每隔 5 秒短促提示音。你也可以在设置中开启“全屏应用时隐藏悬浮层”，彻底隐藏所有悬浮 UI。

**Q：锁屏后状态会丢失吗？**
A：不会。软件监听系统锁屏事件，锁屏时立即进入休息并保存状态；解锁后按活动检测自动恢复。**今日累计的工作 / 休息时长也不会丢失**，只在跨日（过了零点）时才重置。

**Q：设置里能写算式吗？**
A：可以。所有时长类字段都接受算术表达式，例如在“最短工作阈值”里填 `35*60` 表示 35 分钟（2100 秒）。配置文件 `config/settings.json` 会保留 `35*60` 原文，便于你日后人工审阅。支持 `+ - * / ( )`、一元正负号与小数。

**Q：如何切换语言？**
A：打开“设置 → 界面语言”，在 zh-CN 与 en-US 之间切换。设置保存后重启软件生效，语言选择会被持久化保留。若某个语言包文件缺失，软件会自动回退到中文。

**Q：我的数据会上传吗？**
A：**不会**。弹性呼吸是纯本地软件，不写注册表、不联网、不上传任何使用数据。崩溃日志仅写入本机临时目录 `%TEMP%\ElasticBreath\crash\`，便于你自己排查问题。

## 11. 卸载

弹性呼吸是纯便携软件，卸载很简单：

1. 退出正在运行的程序（托盘右键 → 退出，或主窗口关闭时选择“结束运行”）。
2. **删除程序文件夹**即可完成卸载。
3. （可选）若想彻底清理个人配置，删除程序目录下的 `config/` 文件夹。
4. （可选）若曾发生过崩溃，可删除 `%TEMP%\ElasticBreath\` 目录下的崩溃日志。

不会在注册表、开始菜单或系统目录留下任何残留。
